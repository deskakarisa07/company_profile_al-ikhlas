{
    "$schema": "https://railway.com/railway.schema.json",
    "deploy": {
        "startCommand": "if [ ! -e public/storage ]; then php artisan storage:link --force; fi && php artisan serve --host=0.0.0.0 --port=$PORT",
        "restartPolicyType": "ON_FAILURE",
        "restartPolicyMaxRetries": 10
    }
}